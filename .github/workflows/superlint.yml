name: Superlint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  superlint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run Super-Linter
        uses: super-linter/super-linter@v4